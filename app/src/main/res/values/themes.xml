<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Shift" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:fontFamily">@font/golos</item>
        <item name="android:textColorPrimary">@color/blue_200</item>
        <item name="android:colorButtonNormal">@color/blue_200</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Shift" parent="Base.Theme.Shift" />
</resources>